import { ExercisePattern } from './exercise-pattern';

export class ConditionalPattern extends ExercisePattern {
  name = 'conditionalpattern1d';
  records: number;
  repetitions: number;
  unit: string;
}
