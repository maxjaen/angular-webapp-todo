export enum SessionState {
  INITIAL,
  STARTED,
  STOPPED,
}

export enum Period {
  TODAY,
  HISTORY,
}

export enum View {
  PROJECTS,
  PINS,
}

export enum Direction {
  UP,
  DOWN,
}

export enum Pattern {
  FREE,
  WEIGHT,
  COUNTABLE,
  CONDITIONAL1,
  CONDITIONAL2,
}
