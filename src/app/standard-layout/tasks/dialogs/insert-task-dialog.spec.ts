import { InsertTaskDialog } from './insert-task-dialog';

describe('InsertTaskDialog', () => {
  it('should create an instance', () => {
    expect(new InsertTaskDialog(null, null)).toBeTruthy();
  });
});
