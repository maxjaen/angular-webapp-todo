import { InsertTaskDialogTime } from './insert-task-dialog';

describe('InsertTaskDialog', () => {
  it('should create an instance', () => {
    expect(new InsertTaskDialogTime(null, null, null)).toBeTruthy();
  });
});
