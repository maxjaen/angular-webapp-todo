import { ConditionalPattern } from './conditional-pattern';

describe('ConditionalPattern', () => {
  it('should create an instance', () => {
    expect(new ConditionalPattern()).toBeTruthy();
  });
});
