import { ConditionalPattern2d } from './conditional-pattern2d';

describe('ConditionalPattern2d', () => {
  it('should create an instance', () => {
    expect(new ConditionalPattern2d()).toBeTruthy();
  });
});
